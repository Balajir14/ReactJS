import { Button } from "@mui/material";

export default function Mui() {
  return (
    <>
      <Button
        variant="contained"
        sx={{ backgroundColor: "red", borderRadius: "0.3rem", color: "black" }}
      >
        Text
      </Button>
    </>
  );
}
